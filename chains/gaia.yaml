# Cosmos Hub
- name: gaia
  github-organization: cosmos
  github-repo: gaia
  dockerfile: cosmos
  pre-build: |
    apk add libstdc++
  build-target: make install
  build-env:
    - LEDGER_ENABLED=false
    - BUILD_TAGS=muslc
  binaries:
    - /go/bin/gaiad
